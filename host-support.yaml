# Which hosts each plugin ships to. This is the only place that is decided.
#
#   true      ships the plugin to that host
#   a string  withholds it, and is the reason readers see
#
# Every plugin directory must appear here with both hosts stated. scripts/validate.rb
# fails on one that does not, so a plugin cannot reach Codex or Pi by accident, and
# cannot be withheld without a reason. scripts/generate.rb reads this file to write
# the Codex marketplace and the Pi package; neither is edited by hand.
#
# A reason is a claim about another host, so check that host's own documentation
# before writing one. "I have not tried it" is a reason; "it cannot work" needs proof.

align:
  codex: true
  pi: true

clarify:
  codex: true
  pi: true

design-options:
  codex: true
  pi: true

drive-browser:
  codex: true
  pi: >-
    Playwright runs over bash, but the vision loop also needs a browser runtime
    and screenshots returned to the model. Unverified on Pi.

explore:
  codex: true
  pi: true

handoff:
  codex: true
  pi: true

pre-mortem:
  codex: true
  pi: true

preflight:
  codex: true
  pi: true

quality-review:
  codex: >-
    High, xhigh, and max reviews invoke Claude's Workflow tool. A main-model
    fallback would not preserve the finder/verifier contract.
  pi: >-
    High, xhigh, and max reviews invoke Claude's Workflow tool. A main-model
    fallback would not preserve the finder/verifier contract.

reflect:
  codex: true
  pi: true

second-opinion:
  codex: true
  pi: true

silent-failures:
  codex: true
  pi: true

spec:
  codex: true
  pi: true

steel-man-own-position:
  codex: true
  pi: true

transcribe:
  codex: true
  pi: true

understudy:
  codex: true
  pi: true

verify-claims:
  codex: true
  pi: true

verify-result:
  codex: true
  pi: true

worktrunk-hook:
  codex: >-
    The plugin is hooks only, and it needs Claude Code's WorktreeCreate and
    WorktreeRemove events.
  pi: >-
    The plugin is hooks only, and it needs Claude Code's WorktreeCreate and
    WorktreeRemove events.
